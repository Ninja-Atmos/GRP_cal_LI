**Before you use make sure you install Tkinter and custom Tkinter  **


for Tkinter : ```pip install tk```

for tkinter custom no need to install any library, just enter ```import customtkinter``` on your code
